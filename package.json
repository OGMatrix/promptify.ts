{
  "name": "ogmatrix-input",
  "description": "A easy and modern npm package that controls users inputs to prompt the user in cmd.",
  "version": "0.0.4",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "repository": {
    "url": "https://github.com/OGMatrix/ogmatrix-input"
  },
  "scripts": {
    "build": "tsup",
    "push": "npm run build && npm publish",
    "lint": "tsc",
    "release": "pnpm run build && changeset publish"
  },
  "keywords": [
    "input",
    "prompt",
    "ogmatrix"
  ],
  "author": "ogmatrix<matrix@ogmatrix.net>",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "@types/node": "^20.14.10",
    "readline": "^1.3.0",
    "tsup": "^8.1.0",
    "typescript": "^5.5.3"
  }
}
